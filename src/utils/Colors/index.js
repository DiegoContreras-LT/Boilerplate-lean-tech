const DARK_THEME = {
    primaryColor:'red',
};

const LIGHT_THEME = {
    primaryColor:'blue',
};

const DEFAULT_THEME = {
    primaryColor:'yellow',
};

export const THEMES = {
    DARK_THEME,
    LIGHT_THEME,
    DEFAULT_THEME
};

